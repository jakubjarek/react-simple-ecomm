import Wrapper from '../Wrapper/Wrapper';

const ItemsList = ({toCartHandler}) => {
  return (
    <Wrapper>
      <ul className="grid grid-cols-3 gap-4 items-top px-2">
        {/*  */}
        {/*  */}
      </ul>
    </Wrapper>
  );
};

export default ItemsList;
